- [ ] Shift j/k in editor should work as arrow keys, option and super/command key too. option should skip to next blank and command should skip to end/beginning of line

- [ ] in tree shift x should create a pop up that says delete file? This should be the file that was highlighted. No should be highlighted be default and esc should close the promp. j/k should move the yes/no highlight as should the arrow keys. In the pompt box hitting shift x again should select the delete file option.

- [ ] pressing m in tree should create a pop up that allows you to select where to move the highlighted file

- [ ] when pressing n for a new file, I don't like that it is down at the bottom. Instead, make a pop up with the naming prompt

- [ ] need a reload button where it scans the file folders for changes. Should we be polling on a regular basis? I don't want to add background processes if that isn't a good idea, but if it's not out of the norm and won't cause any issues I am down. Give me your best thoughts on it


