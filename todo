- [x] Live markdown styling in the editor (the big one) @done(2026-08-31 20:14)
  Shipped: custom renderer over the textarea engine (render.rs + pure
  highlight.rs tokenizer + wrap.rs). Full markdown set, HTML tags, and
  YAML frontmatter; marks stay visible, dimmed. Mark-hiding on
  non-cursor lines deliberately deferred — it can layer on later.

- [ ] Harden for v0.1 (review follow-ups) — DO THIS BEFORE SHIPPING
  Plan: docs/superpowers/plans/2026-08-31-mrkdup-review-followups.md
  Order: unred Windows CI (slash paths) → preserve CRLF → root
  gitignore → Ctrl+Q in prompts → [X]/single-undo checkbox → split
  app.rs (fuzzy/search/checkbox) → cache wrap+highlight. No new
  features. Mark-hiding still deferred.

- [ ] Ship it properly: crates.io + releases
  Blocked on the harden plan above. Then: publish `mrkdup` to
  crates.io (grabs the name), tag v0.1.0, add a CI badge to the
  README, and set up cargo-dist (or a release workflow) so tagged
  releases ship prebuilt binaries for mac/linux/windows.

- [ ] Themes (after harden Package C; not a v0.1 blocker)
  Plan: docs/superpowers/plans/2026-08-31-mrkdup-themes.md
  Extract Theme (only Color:: lives there) → theme = default|light|mono
  → user overlay file + named themes/<name>. No new keys, no new
  crates, no per-project config, no live reload.
